export enum AppRoutes {
	Main = 'main',
	Agency = 'agency',
	Services = 'services',
	Cases = 'cases',
	Blog = 'blog',
	Contacts = 'contacts',
	Product = 'product',
	Products = 'products',
}

export interface BreadcrumbLinkConfig {
	href: string;
	title: string;
}
